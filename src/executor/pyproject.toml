[project]
name = "rosetta-executor"
version = "0.1.0"
description = "Subprocess sandbox executor microservice for rosetta-decode"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "pandas>=2.2",
    "openpyxl>=3.1",
    "pyreadstat>=1.2",
    "httpx>=0.27",
]
